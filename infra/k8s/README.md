# Kubernetes Placeholder

This folder will hold Kubernetes manifests or Helm assets after the local vertical slice is stable.

Planned assets:
- API deployment and service
- Worker deployment
- Ingress and TLS config
- Secret and config templates
- Migration job
